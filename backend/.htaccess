RewriteEngine On

# Deploy script exclusion
RewriteCond %{REQUEST_URI} ^/deploy\.php$
RewriteRule ^ - [L]

# Proxy all other requests to Node.js on port 5000
RewriteRule ^(.*)$ http://127.0.0.1:5000/$1 [P,L]
