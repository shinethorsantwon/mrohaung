{
    "name": "mrohaung-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "node backend/index.js",
        "build": "echo 'No build script for root'",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {},
    "engines": {
        "node": ">=18.0.0"
    }
}